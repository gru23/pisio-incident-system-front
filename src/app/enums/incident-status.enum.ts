export enum IncidentStatus {
    Reported = "REPORTED",
    Pending = "PENDING",
    Approved = "APPROVED",
    Rejected = "REJECTED",
    Resolved = "RESOLVED",
    Duplicate = "DUPLICATE",
    Canceled = "CANCELED"
}